function solution(arr, intervals) {
  var answer = [];
  for (let i = 0; i < intervals.length; i++) {
    a = intervals[i][0];
    b = intervals[i][1];
    answer.push(...arr.slice(a, b + 1));
  }
  return answer;
}
//배열만들기
