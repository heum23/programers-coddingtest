// 특정 문자열로 끝나는 가장 긴 부분 문자열 찾기
function solution(myString, pat) {
  var answer = "";
  let arr = myString.split("");
  let num = myString.lastIndexOf(pat);
  let kal = num + pat.length;
  console.log(num);
  answer = myString.slice(0, kal);
  return answer;
}
